# ORAN CFM-Lite AI Anomaly Detection System
# Updated: 2025-11-05
# Optimized for ML pipeline (BFD, BGP, PTP, CFM)

# ============================================================================
# CORE DEPENDENCIES (필수)
# ============================================================================

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Data Validation
pydantic==2.5.1
pydantic-settings==2.1.0

# Configuration
pyyaml==6.0.1

# CLI
typer==0.9.0
rich==13.7.0

# ============================================================================
# DATA PROCESSING & ML (필수)
# ============================================================================

# Core Data Science (버전 범위로 업데이트)
numpy>=1.24.0,<3.0.0
pandas>=2.0.0,<3.0.0

# Machine Learning
scikit-learn>=1.3.0,<2.0.0

# Graph Processing (BGP GNN용) - 새로 추가
networkx>=3.0,<4.0

# Data Formats (Parquet) - 새로 추가
pyarrow>=14.0.0

# Visualization (리포트 생성용) - 새로 추가
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0

# Time Series Analysis
ruptures>=1.1.8,<2.0.0
statsmodels>=0.14.0,<1.0.0
scipy>=1.11.0,<2.0.0

# ============================================================================
# DEEP LEARNING (선택적 - 주석 처리됨)
# ============================================================================

# PyTorch (LSTM, GNN, TCN용)
# torch>=2.0.0,<3.0.0
#
# 설치 방법:
#   CPU 버전: pip install torch --index-url https://download.pytorch.org/whl/cpu
#   GPU 버전: pip install torch

# ============================================================================
# NETWORK MONITORING (ORAN 장비 연동용, 선택적)
# ============================================================================

# NETCONF/YANG
ncclient>=0.6.15,<1.0.0
paramiko>=3.0.0,<4.0.0
xmltodict>=0.13.0,<1.0.0
lxml>=4.9.0,<5.0.0

# ============================================================================
# LOGGING (선택적)
# ============================================================================

# Structured Logging
structlog>=23.0.0,<24.0.0

# ============================================================================
# DEVELOPMENT & TESTING
# ============================================================================

# Testing
pytest>=7.4.0,<8.0.0
pytest-asyncio>=0.21.0,<1.0.0
pytest-cov>=4.1.0,<5.0.0

# Code Quality
black>=23.0.0,<24.0.0
ruff>=0.1.0,<1.0.0
mypy>=1.7.0,<2.0.0

# ============================================================================
# REMOVED / OPTIONAL (현재 사용 안 함, 주석 처리)
# ============================================================================

# TensorFlow (제거됨 - 1.5GB+, 현재 미사용)
# tensorflow>=2.14.0

# Message Queue (제거됨 - 현재 사용 안 함)
# kafka-python==2.0.2
# redis==5.0.1
# celery==5.3.4

# Database (제거됨 - 현재 사용 안 함)
# asyncpg==0.29.0
# sqlalchemy[asyncio]==2.0.23
# alembic==1.13.1

# Additional ML (제거됨 - 현재 사용 안 함)
# xgboost>=2.0.0
# lightgbm>=4.0.0

# Monitoring (제거됨 - 현재 사용 안 함)
# prometheus-client==0.19.0
# opentelemetry-api==1.21.0
# opentelemetry-sdk==1.21.0

# HTTP Client (제거됨 - 현재 사용 안 함)
# httpx==0.25.2
# python-multipart==0.0.6
